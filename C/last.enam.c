#include<stdio.h>

enum week{monday=1, tuseday, wednesday, thursday, friday, staurday, sunday};

int main ()
{
    
    printf("%d, %d, %d, %d, %d, %d, %d",monday, tuseday, wednesday, thursday, friday, staurday, sunday);

    return 0;
}